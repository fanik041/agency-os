{
  "name": "@agency-os/scraper-service",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --noEmit && npx playwright install --with-deps chromium",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@agency-os/db": "workspace:*",
    "dotenv": "^17.3.1",
    "express": "^4.19.0",
    "playwright": "^1.44.0",
    "tsx": "^4.11.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.0"
  }
}
